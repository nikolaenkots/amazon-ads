# Расписание второй установки. Пути под VM margoads
# (/home/nikolaenko_ts/margoads); для другой машины поправьте путь.
# Ставится так:  crontab ~/margoads/deploy/crontab.txt
# Время в UTC (проверить: timedatectl). 07:00 UTC = 09:00 в Варшаве зимой.
#
# Синхронизация структуры кампаний — раньше сбора статистики,
# чтобы отчёты ложились на актуальные кампании и группы.
0 6 * * * cd $HOME/margoads && venv/bin/python scripts/auto_sync_campaigns.py >> $HOME/margoads/logs/sync.log 2>&1

# Сбор статистики за последние 14 дней по всем гео и типам отчётов
0 7 * * * cd $HOME/margoads && venv/bin/python scripts/auto_collect.py >> $HOME/margoads/logs/collect.log 2>&1

# Отправка одобренных изменений в Amazon — каждый час
30 * * * * cd $HOME/margoads && venv/bin/python scripts/send.py --account MERCH >> $HOME/margoads/logs/send.log 2>&1
35 * * * * cd $HOME/margoads && venv/bin/python scripts/send.py --account KDP   >> $HOME/margoads/logs/send.log 2>&1
